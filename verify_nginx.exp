#!/usr/bin/expect

set timeout 20
set password "1234"

spawn ssh root@212.90.120.135 "grep -n 'return 301' /etc/nginx/sites-enabled/ecokambio"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
catch wait result
exit [lindex $result 3]
