#!/usr/bin/expect

set timeout 20
set password "1234"

spawn ssh root@212.90.120.135 "pwdx \$(pgrep -f 'server.js' | head -n 1)"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
