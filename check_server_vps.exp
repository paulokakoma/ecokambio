#!/usr/bin/expect

set timeout 20
set password "1234"

spawn ssh root@212.90.120.135 "head -5 /root/ecokambio/server.js && wc -l /root/ecokambio/server.js"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
