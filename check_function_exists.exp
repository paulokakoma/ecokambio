#!/usr/bin/expect

set timeout 20
set password "1234"

spawn ssh root@212.90.120.135 "grep 'const enforceHttps =' /root/ecokambio/server.js"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
catch wait result
exit [lindex $result 3]
