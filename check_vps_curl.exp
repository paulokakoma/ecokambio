#!/usr/bin/expect

set timeout 20
set password "1234"

spawn ssh root@212.90.120.135 "curl -I http://localhost:3000"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
