#!/usr/bin/expect

set timeout 20
set password "1234"

# Upload via cat
spawn bash -c "cat server.js | ssh root@212.90.120.135 'cat > /root/ecokambio/server.js'"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}

# Restart PM2
spawn ssh root@212.90.120.135 "pm2 restart all"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
