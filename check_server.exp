#!/usr/bin/expect

set timeout 20
set password "1234"

spawn ssh root@212.90.120.135 "cat /root/ecokambio/server.js | grep enforceHttps"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
