#!/usr/bin/expect

set timeout 20
set password "1234"

spawn ssh root@212.90.120.135 "echo '--- PM2 LIST ---' > /root/diag.txt && pm2 list >> /root/diag.txt && echo '--- FILE LS ---' >> /root/diag.txt && ls -l /root/ecokambio/server.js >> /root/diag.txt && echo '--- GREP ---' >> /root/diag.txt && grep 'enforceHttps' /root/ecokambio/server.js >> /root/diag.txt && cat /root/diag.txt"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
