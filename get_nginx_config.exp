#!/usr/bin/expect

set timeout 20
set password "1234"

spawn ssh root@212.90.120.135 "cat /etc/nginx/sites-enabled/* > /root/nginx_config.txt && cat /root/nginx_config.txt"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
