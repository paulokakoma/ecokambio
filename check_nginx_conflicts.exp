#!/usr/bin/expect -f

set timeout 20
set password "1234"
set server "212.90.120.135"

spawn ssh -o StrictHostKeyChecking=no root@$server
expect "password:"
send "$password\r"
expect "#"

puts "\n=== LISTANDO SITES ENABLED ===\n"
send "ls -l /etc/nginx/sites-enabled/\r"
expect "#"

puts "\n=== CONTEÚDO ECOKAMBIO ===\n"
send "cat /etc/nginx/sites-available/ecokambio\r"
expect "#"

interact
