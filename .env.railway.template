# ============================================
# RAILWAY ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Copy this file and fill with your actual values
# Railway: Settings > Variables > Raw Editor > Paste

# ============================================
# SERVER CONFIGURATION
# ============================================
NODE_ENV=production
PORT=3000

# ============================================
# SUPABASE CONFIGURATION (Required)
# ============================================
# Get these from: https://supabase.com/dashboard > Project Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Alternative variable name (Railway accepts both)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# ADMIN AUTHENTICATION (Required)
# ============================================
# Generate hash: node scripts/hash-password.js
# Or use bcrypt online: https://bcrypt-generator.com/ (use cost factor 12)
ADMIN_PASSWORD_HASH=$2b$12$your-hashed-password-here

# ============================================
# SESSION CONFIGURATION (Required)
# ============================================
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=your-random-secret-here-minimum-32-characters

# ============================================
# COOKIE CONFIGURATION (Production)
# ============================================
# Leave empty for Railway's default .railway.app domain
# Update to .ecokambio.com when custom domain is configured
COOKIE_DOMAIN=

# ============================================
# DATABASE (Optional - for session persistence)
# ============================================
# Railway PostgreSQL plugin sets this automatically
# Or use Supabase connection string for sessions:
# DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres

# ============================================
# ADMIN PATH (Optional)
# ============================================
# Custom secret path for admin access
# Default: /admin
ADMIN_SECRET_PATH=/acesso-admin-secreto-123

# ============================================
# STORAGE (Optional - for image uploads)
# ============================================
# Only needed if using Contabo/S3 for image uploads
# Otherwise, Railway's ephemeral storage is used
# CONTABO_ENDPOINT=https://eu2.contabostorage.com
# CONTABO_ACCESS_KEY_ID=your-access-key
# CONTABO_SECRET_ACCESS_KEY=your-secret-key
# CONTABO_BUCKET_NAME=your-bucket-name

# ============================================
# PLAYWRIGHT (Automatic - Don't modify)
# ============================================
# These are set automatically in railway.toml
# PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=0
# NODE_ENV=production
